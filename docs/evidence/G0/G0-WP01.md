---
id: EV-G0-WP01
kind: evidence-record
gate: G0
work_package: G0-WP01
status: passed
date: 2026-08-07
---

# G0-WP01 evidence

## Delivered

- Safe nested Git root `ReFusion/` created because the parent workspace name
  ends with a trailing space.
- Research draft copied without deleting the original.
- Repo authority docs, Master Plan, status, G0/G1 plans, ADRs, templates, Agent
  guidance and repo-local skill created.

## Verification

| Check | Result |
|---|---|
| Repo-local Skill validator | Passed: `Skill is valid!` |
| `python3 tools/rfdev.py docs-doctor` | Passed, 16 normative documents, zero problems |
| `python3 tools/rfdev.py architecture-check` | Passed, zero forbidden boundary tokens |
| `python3 tools/bootstrap.py doctor` | CMake 4.3.3, Ninja 1.13.2, Qt 6.11.1 detected |
| Existing official Skia checkout verification | Origin and `76941bbcaf48f2927878038412a6a69db0f45449` matched lock |
| `cmake --workflow --preset macos-core` | Configure/build/test passed |
| `cmake --build --preset macos-studio` | Qt Studio target built successfully |
| `ctest --preset macos-studio` | 1/1 test passed |
| Studio offscreen startup smoke | Clean startup; terminated after bounded smoke interval |
| Foundation CLI | Accepted command and published revision 2 |

The Skill validator's `PyYAML` dependency was installed only into a temporary
validation directory; it was not added to ReFusion product dependencies.

## Baseline

Initial foundation commit: `1a70f5a`.

Windows, native GPU, media, packaging, signing, and real-device qualification
remain future gate evidence and are not implied by this record.

