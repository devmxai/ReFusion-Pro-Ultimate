---
id: EV-G0-WP03
kind: evidence-record
gate: G0
work_package: G0-WP03
status: code-complete-awaiting-windows-runner
source_commit: dba78d8
date: 2026-08-07
---

# G0-WP03 evidence

## Delivered contract

- Strong engine-owned `ProjectId`, `RevisionId`, `CommandId`, and
  `IdempotencyKey` value types.
- One `CommandEnvelope` carrying command identity, expected revision, and
  idempotency identity for UI, CLI, and future Agent/MCP adapters.
- Deterministic `accepted`, `replayed`, and `rejected` outcomes with command and
  committed-revision receipts.
- Fail-closed diagnostics for missing identity, idempotency-key reuse,
  command-ID reuse, stale revisions, invalid schema, and revision overflow.
- Late idempotent replay returns the current active Snapshot while preserving
  the original committed revision, preventing UI state regression.
- Mutex-serialized acceptance guarantees that simultaneous commands targeting
  one expected revision produce exactly one accepted successor.

## macOS arm64 evidence

Host: macOS 26.5.1 arm64, Apple Clang 21.0.0, CMake 4.3.3, Ninja 1.13.2.

- `cmake --workflow --preset macos-core`: configure/build/test passed, 1/1.
- `cmake --workflow --preset macos-studio`: Qt Studio and Core passed, 1/1.
- Debug concurrency/LKG test repeated 100 times in Core and 25 times in Studio
  with zero failure.
- An independent Release build passed, proving tests do not depend on `assert`.
- AddressSanitizer plus UndefinedBehaviorSanitizer build/test passed.
- CLI emitted accepted command `cmd_foundation_cli_demo_1` at revision 2.

## Structural evidence

- Docs Doctor passed 24 normative documents with zero problems.
- Architecture check scanned six owned source files, found only the five
  allowlisted Qt runtime targets, and reported zero problems.
- Dependency policy JSON parsed successfully.
- Git diff whitespace validation passed.

## Portable CI definition

`.github/workflows/portable-core.yml` defines:

- macOS 15 arm64 with the `macos-core` workflow preset;
- Windows Server 2025 x64/MSVC with the `windows-core` workflow preset;
- MSVC discovery through vendor `vswhere`, with no hard-coded VS release path;
- immutable `actions/checkout` commit
  `3d3c42e5aac5ba805825da76410c181273ba90b1` (v7.0.1);
- read-only repository permission and disabled persisted checkout credentials;
- toolchain, documentation, architecture, configure, build, and test gates.

The YAML parses locally. The repository has no remote configured, so no honest
Windows execution evidence exists yet.

## Remaining acceptance item

Run the committed workflow on an actual Windows x64 runner, retain the run URL,
runner image and compiler versions, and require 1/1 tests passing. Compile-only
Windows evidence will close G0-WP03; it will not qualify Windows GPU/media
runtime behavior, which belongs to G1.
