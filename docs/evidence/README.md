# Evidence

Evidence is immutable output linked to a work package, commit, dependency lock,
toolchain, platform/device profile, commands, and artifacts. Compile evidence
does not substitute for runtime, GPU, media, signing, or clean-machine evidence.

Use `docs/templates/CHECKPOINT.md` for resumable checkpoints and
`docs/templates/HANDOFF.md` when transferring work.

