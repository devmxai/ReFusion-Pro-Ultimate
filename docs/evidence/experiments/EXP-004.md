---
id: EV-EXP-004
kind: evidence-record
gate: pre-G3-experiment
work_package: EXP-004
status: implementation-passed-owner-evaluation-revision-required
source_commit: working-tree-pending-checkpoint
date: 2026-08-08
---

# Modern Shape and Background authoring evidence

Portable Core owns `ShapeFill`, gradient stops, border, blend and creation
presets. Studio projects Registry values and submits typed commands; it does not
construct project objects. Accepted snapshots activate Runtime and canonical
RFX3 persistence together. RFX1/RFX2 remain readable.

Verification receipts:

- `python3 tools/rfdev.py docs-doctor`: 83 documents, zero problems;
- `python3 tools/rfdev.py architecture-check`: 58 owned source files, zero
  problems;
- `macos-core`: 10/10 passed;
- `macos-core-sanitized`: 10/10 passed under ASan/UBSan;
- `macos-studio`: 15/15 passed;
- `macos-visual`: 24/24 passed.

The Visual lane includes Core, Studio, workspace, live-reload, Metal presenter,
Skia adapter and native decoded-surface tests.

Cross-platform semantic state is portable C++ and contains no Qt/Skia/platform
objects. Physical evidence is macOS-only; Windows remains not-run.

Owner evaluation on 2026-08-08 accepted the visible modern Background progress
but found that its internal animated components were authored as independent
root Layers and that Text-to-Shape alignment remained unmeasured. The build and
automated receipts above remain valid; the owner outcome is revision required,
routed by EV-VA-0001 to EXP-006. No G3 or cross-platform completion is claimed.
