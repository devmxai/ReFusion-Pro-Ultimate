---
id: CP-G1-0005
kind: checkpoint
gate: G0-with-bounded-G1-macos-runtime
work_package: G1-WP01,G1-WP03
commit: 79aafb072ad0f254ef64726231b2e7c7e50b5fe8
date: 2026-08-07
state: exact-walking-transport-controls-and-playhead-passed-macos
---

# Completed

- Added portable typed Play, Pause and Seek-to-frame transport commands and an
  immutable playback snapshot with frame index/count.
- Preserved the paused frame and rebuilt the playback origin on resume, instead
  of resetting to frame zero.
- Added checked rational frame/time mapping, including a `30000/1001` boundary
  test that selects the first representable nanosecond of the requested frame.
- Added a separate Qt `StudioTransportBridge`; QML submits intents and displays
  snapshots but owns no clock, timer or project transport state.
- Replaced hard-coded Timeline rows with project-derived Layer IDs and active
  ranges, a time ruler, centered Play/Pause control and a real overlay playhead.
- Enabled paused click/drag seek with immediate Skia Canvas reevaluation.

# Verification

- `macos-core`: 6/6 passed.
- `macos-core-sanitized`: 6/6 passed under ASan+UBSan.
- `macos-studio`: 8/8 passed.
- `macos-visual`: 13/13 passed.
- `macos-media`: 9/9 passed.
- Architecture Check and Docs Doctor: zero problems.
- Presenter soak: 10,000/10,000; CPU pixel maps/uploads, GPU readbacks and
  unattributed copies all exactly zero.
- Physical macOS: pause remained stationary for two seconds, resume continued
  from the frozen frame, click and drag moved the playhead and reevaluated the
  Canvas while paused. The final screenshot shows `PAUSED` and `GPU READY`.

# Claim boundary

This qualifies the bounded Shape/Text walking transport on macOS. It does not
qualify decoded Video Layer playback, audio master clock, source PTS selection,
full G4 transport, Windows, iOS or Android.

# Exact resume point

Return to active G1-WP03: decode the bounded H.264 sample through a
hardware-required VideoToolbox session, retain its PTS/color and engine surface
lease, then feed the decoded surface into this frame-addressed transport without
introducing a second clock or any CPU pixel path.
