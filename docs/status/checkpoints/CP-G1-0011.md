---
id: CP-G1-0011
kind: checkpoint
gate: G0-with-bounded-G1-desktop-runtime
work_package: G1-WP02,PLAN-XPLAT-FIX-001
commit: 47948b29525ce8bf42aaab5e021d2bf32e6e6338
date: 2026-08-09
state: windows-canvas-quality-macos-qualified-promotion-ready
tag: checkpoint-xplat-canvas-quality-20260809
---

# Completed

- Reconciled `fix/windows-canvas-quality` without allowing the Windows host to
  mutate `main`.
- Retained one full-resolution, high-precision common Skia presentation route;
  Metal and D3D12 select only native target/profile mechanics.
- Qualified the affected macOS Core, Graphics, Visual and physical Metal paths.
- Corrected ADR identity collisions and restored the dependency tamper negative
  test to the cache-aware resolver.

# Verification

- macOS Core: 31/31 passed.
- macOS Graphics: 40/40 passed.
- macOS Visual: 52/52 passed.
- Physical Studio selected RGBA16F/linear-sRGB on Apple M1, survived transport
  and resize, and recorded no device-loss rejection.
- Physical 240-frame Metal stress: 240 presentations, zero CPU maps/uploads,
  GPU readbacks or unattributed copies.
- Architecture and documentation checks: zero problems.

# Claim boundary

This checkpoint admits promotion of the reviewed Canvas-quality correction. It
does not claim a complete cross-platform product, Windows Video import/decode,
same-commit calibrated backend pixel equivalence, Android runtime, Export,
installer or release qualification.

# Exact resume point

Promote this integration descendant to `main`, tag the reviewed checkpoint,
then merge that exact `main` into `feature/shared-video-import-v1`. Rerun the
Video Import macOS demux lane before continuing its next work package; never
reapply the Windows correction manually inside the feature branch.
