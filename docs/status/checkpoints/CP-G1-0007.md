---
id: CP-G1-0007
kind: checkpoint
gate: G0-with-bounded-G1-macos-runtime
work_package: G1-WP03
commit: a697c6a873760366bb957590038cbf3416e644d0
date: 2026-08-07
state: core-clock-multiframe-pts-gpu-selection-passed-macos
---

# Completed

- Added portable Core `ProjectClock` as the sole mutable ProjectTime authority.
  Play/Pause/Stop/Seek/sample state, source generation, loop index and transport
  epoch no longer live in the viewport scheduler.
- Accepted ADR-0009 and hardened the architecture/Master Plan rule: physical
  clock sources provide correlated ticks only; UI, presenter, video and audio
  clips never own a second project clock.
- Added exact checked frame/time mapping, monotonic/source-generation rejection,
  pause/resume loop continuity and epoch-stamped `FixtureFrame` snapshots.
- Replaced the one-frame/one-session decoder slice with one hardware-required
  VideoToolbox session submitting all eight compressed access units.
- Published eight opaque same-device NV12/Metal leases through one immutable
  exact-PTS-indexed queue and added the matching portable Windows fail-closed
  method.
- Made Skia wrap all queue surfaces as GPU YUVA images and select the active one
  exclusively from the Core ProjectTime carried by engine transport.

# Verification

- `macos-core`: 7/7 passed.
- `macos-core-sanitized`: 7/7 passed under ASan+UBSan.
- `macos-media`: 12/12 passed.
- `macos-visual`: 18/18 passed.
- Architecture Check: 43 owned files, zero problems.
- Docs Doctor: 60 documents, zero problems.
- Hardware receipt: one VideoToolbox session, eight compressed samples, eight
  decoded frames, one flush, one immutable queue, 16 plane bindings and eight
  deterministically released leases.
- Core seeks `[3, 7, 1]` produced ProjectTime
  `[100000000, 233333334, 33333334]` ns and Skia selected source frames
  `[3, 7, 1]` at transport epochs `[1, 2, 3]`.
- Software decoder selections, CPU pixel maps/conversions/uploads, GPU
  readbacks, cross-adapter events and unattributed GPU copies: all zero.

# Claim boundary

This proves one central Core clock driving exact PTS selection across an
all-IDR bounded H.264 GPU queue on physical macOS. It is not yet an audio-device
clock, MP4 demux/import, B-frame/VFR/long-GOP seek, production Timeline Video
Layer playback, drift/route-change qualification, export, or Windows/iOS/Android
runtime qualification.

# Exact resume point

Continue `G1-WP03` with a deterministic B-frame/VFR/non-zero-PTS/long-GOP corpus.
Add decode-order versus presentation-order tests, dependency-aware seek/flush,
queue invalidation on transport epoch/device generation changes and bounded
residency/backpressure. Do not add an audio endpoint clock until its G4
ClockSource correlation and drift policy are explicitly scoped.
