---
id: CP-G1-0004
kind: checkpoint
gate: G0-with-bounded-G1-macos-runtime
work_package: G1-WP01,G1-WP03
commit: ac8de22c329a64a9f351796ae5ded280e701984c
date: 2026-08-07
state: macos-presenter-accepted-media-capability-interop-passed-decode-pending
---

# Owner direction

The product owner reported the manual G1-WP01 sleep/wake path working and made
retention of the automated sleep receipt non-blocking for current development.
The report is recorded without being relabeled as automated lab evidence.
G1-WP03 is admitted as the next macOS physical-runtime package while Windows
continues to be designed in parallel and recorded as `not-run`.

# Completed

- Added portable exact media timing, strict H.264/NV12/SDR Rec.709 capability
  and zero-fallback/CPU-transfer/cross-adapter counter contracts.
- Added explicit `macos-media` and `windows-media` configure/build/test lanes.
- Implemented the Apple H.264 hardware capability query.
- Allocated a two-plane NV12 native surface and bound both planes to Metal
  textures on the engine-owned adapter ID/generation without mapping pixels.
- Made invalid profiles and suspended GPU state fail closed with typed codes.
- Added a deliberate Windows not-qualified implementation pending G1-WP04.

# Verification

- `macos-media`: 9/9 passed.
- `macos-core`: 6/6 passed.
- `macos-core-sanitized`: 6/6 passed under ASan+UBSan.
- `macos-visual`: 13/13 passed.
- Architecture Check: 35 files, zero problems.
- Every forbidden media-path counter remained exactly zero.

# Exact resume point

Create a bounded repository-owned H.264 fixture with known presentation timing,
then create a VideoToolbox decompression session that explicitly requires
hardware acceleration. Retain decoded output PTS/color and an engine-owned
native-surface lease, bind its two planes to the same Metal device, and fail
closed on unsupported/session/output/lifetime errors. Do not integrate this
probe into Video Layer/import/Timeline semantics before those receipts pass.
