---
id: CP-G1-0015
kind: checkpoint
gate: G0-with-bounded-video-vertical-slice
work_package: PLAN-VIDEO-VS-001,VI-WP03
commit: 0d0badc
date: 2026-08-09
state: video-import-wp03-macos-complete-msvc-pending
---

# Completed

- Added typed product CLI clients for ImportVideo and exact-byte Relink over the
  same Application services used by Studio.
- Kept persistence fail-closed: accepted in-memory state is CAS-persisted before
  prepared Asset bytes are retained; persistence rejection rolls them back.
- Exposed explicit media-command capabilities and generated Agent guidance that
  forbids hand-authoring media truth.
- Preserved native/Unicode paths without putting host paths in canonical RFX6.

# Verification

- Full macOS Core: 37/37 passed.
- Full macOS Visual: 62/62 passed.
- Physical CLI receipt imports the real corpus, verifies path-free RFX6, deletes
  the copied original, restores it exactly, proves project bytes unchanged and
  validates canonical reopen.
- Architecture: 132 source files, zero problems and zero visual-boundary debt.

# Claim boundary

This completes VI-WP03 locally on macOS only. It does not claim an MSVC receipt,
Timeline Video/Audio projection, decoded Canvas frame, hardware decode, Audio
playback or VI-WP04.

# Exact resume point

Run VI-WP01–03 RFX6, ImportVideo, exact Relink, Studio client, typed CLI and
recovery receipts under real MSVC/Windows from source commit `0d0badc` or its
document-only successor. Do not create a Windows semantic/filesystem fork. Only
after those receipts are green may VI-WP04 exact scheduling and
`DrawVideoFrame` begin.
