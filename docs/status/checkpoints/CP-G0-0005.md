---
id: CP-G0-0005
kind: checkpoint
gate: G0
work_package: G0-WP04A,G0-WP04B,G0-WP05
commit: 24d5946e442de09ac9ccc798f9e7aeedeee04502
date: 2026-08-07
state: authority-passed-local-supply-chain-passed-external-gates-pending
---

# Completed

- Moved mutable project authority behind one private Application Host; Studio
  and CLI now submit the same typed commands through `ProjectCommandService`.
- Added accepted/rejected command integration coverage, Last-Known-Good
  diagnostic semantics, negative authority tests, and sanitizer coverage.
- Bound the repository-local Skia graph, 46 transitive revisions, GN profile,
  toolchain fingerprints and built artifact into fail-closed records.
- Added a Qt Commercial release-admission gate and rejected out-of-tree or
  unapproved SDK input.
- Split G1 into eight bounded proof packages and named the macOS lab device.

# Evidence

- `docs/evidence/G0/G0-WP04A.md`
- `docs/evidence/G0/G0-WP04B.md`
- `docs/evidence/G0/G0-WP05.md`
- Source commit: `24d5946e442de09ac9ccc798f9e7aeedeee04502`.
- Clean combined build: 6/6 tests passed.
- macOS Core sanitizer build: 3/3 tests passed under ASan+UBSan.
- Docs Doctor, Architecture Check, exact toolchain doctor, full 46-dependency
  Skia verification, JSON/YAML parsing and whitespace validation passed.

# Known failures and external gates

- G0-WP03 still needs a real Windows x64 Core run with URL/image/compiler proof.
- Windows same-device D3D/Dawn Skia and native media-surface paths are not
  implemented or qualified; the graphics configure intentionally fails closed.
- Qt Commercial entitlement/SDK admission, independent clean release rebuild,
  SBOM/notices, signing and notarization receipts are unavailable locally.
- Therefore G0 and G1 are not marked passed.

# Exact resume point

Attach a clean Windows x64 runner first. Run the portable Core workflow, record
the runner/compiler evidence, then hydrate the official repository-local Skia
graph and generate the Windows lock. Implement and prove the D3D/Dawn
same-device context before enabling `windows-graphics`. In parallel, place the
approved Qt Commercial 6.11.1 SDK under the ignored release-admission directory
and supply the private receipt/signing owners. Finish G0-WP05 only after these
receipts exist.

# Work that must not be repeated

- Do not reopen or duplicate the Application Host authority correction.
- Do not reuse an external Skia checkout or weaken repository-local path checks.
- Do not claim Windows, Qt Commercial redistribution, signing, packaging,
  mobile, renderer, media or export qualification from the macOS local proof.
