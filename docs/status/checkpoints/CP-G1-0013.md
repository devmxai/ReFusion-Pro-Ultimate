---
id: CP-G1-0013
kind: checkpoint
gate: G0-with-bounded-video-vertical-slice
work_package: PLAN-VIDEO-VS-001,VI-WP03
commit: 3df1719
date: 2026-08-09
state: studio-video-import-client-persistence-macos-passed-relink-cli-msvc-pending
---

# Completed

- Connected Studio `VID` to the native file portal and the one shared
  `ImportVideoService`; QML emits only the selected-file intent.
- Added a bounded background client for fingerprint, demux/index and verified
  project-asset copy, with only snapshot lookup and final Revision admission
  marshalled to the engine thread.
- Reused the accepted-workflow observer to persist canonical RFX6 and refresh
  Studio projections after one accepted Revision.
- Added progress, cancellation and typed final diagnostics.
- Fixed a reproduced worker-stack overflow by moving the bounded 1 MiB copy
  buffer from thread stack to heap storage.

# Verification

- macOS Visual: 61/61 passed.
- Studio client physical import: selected real MP4 -> shared index/copy -> one
  linked Video/Audio Revision -> persisted and reopened RFX6.
- Focused import/service/live-reload/client tests: 4/4 passed.
- Architecture: 129 source files, zero problems and zero visual-boundary debt.

# Claim boundary

This checkpoint proves the macOS Studio client and accepted-file persistence.
It does not claim Timeline Video/Audio rows, decoded Canvas frames, Audio output,
typed relink, CLI parity, Windows MSVC reproduction or VI-WP03 closure.

# Exact resume point

Continue on `feature/shared-video-import-v1` with typed exact-byte relink and a
CLI client over the same shared workflow boundary. Reproduce VI-WP01–03 under
MSVC before beginning VI-WP04 exact scheduling and `DrawVideoFrame`.
