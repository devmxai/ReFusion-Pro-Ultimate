---
id: CP-G0-0003
kind: checkpoint
gate: G0
work_package: G0-WP03
commit: dba78d8
date: 2026-08-07
state: code-complete-awaiting-windows-ci
---

# Completed

- Recorded the product owner's Qt Commercial choice and passed G0-WP02.
- Added typed command identity, expected-revision and idempotency envelopes.
- Added deterministic acceptance/replay/rejection receipts and fail-closed
  Last-Known-Good behavior.
- Added concurrency, replay, validation, overflow and constructor tests.
- Updated CLI and Qt Studio adapters to submit the same Core command contract.
- Added least-privilege portable-Core CI for macOS arm64 and Windows x64.

# Evidence

See `docs/evidence/G0/G0-WP03.md` for local toolchains, commands, test counts,
sanitizer results, CI definition and the deliberately unclaimed Windows result.

# Decisions changed

- ADR-0005 is accepted with `commercial` as the distribution lane.
- The Qt policy lock now rejects an unselected or implicit license lane.

# Known failures and risks

- No Git remote or Windows runner is connected, so Windows Core execution is
  pending and G0-WP03 remains active.
- RISK-002 remains open for the G1 GPU ownership/interoperability bake-off.
- Qt Commercial entitlement evidence is still required at the release gate;
  selecting the lane is not proof of purchase or redistribution rights.

# Exact resume point

Publish this repository to a CI-capable remote or attach a Windows x64 runner,
execute `.github/workflows/portable-core.yml`, and store the run URL plus
toolchain/image evidence. If green, mark G0-WP03 passed and activate G0-WP04.

# Commands already run

- macOS Core and Studio workflows.
- Release Core build/test.
- AddressSanitizer and UndefinedBehaviorSanitizer Core build/test.
- 100 Core and 25 Studio repeat-until-fail test executions.
- Docs Doctor, architecture check, JSON parsing, workflow YAML parsing and
  whitespace validation.

# Work that must not be repeated

- Do not redesign the command envelope before using this baseline in the next
  work package; changes require a failing requirement or test.
- Do not claim Windows platform qualification from hosted compile CI.
- Do not reopen the Qt licensing lane without a superseding ADR.
