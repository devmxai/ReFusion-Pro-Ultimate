---
id: CP-G1-0014
kind: checkpoint
gate: G0-with-bounded-video-vertical-slice
work_package: PLAN-VIDEO-VS-001,VI-WP03
commit: 872d36b
date: 2026-08-09
state: exact-asset-relink-macos-passed-cli-msvc-pending
---

# Completed

- Added one typed `ExactAssetRelinkService` over the same immutable-source and
  rollback-owned materialization boundary used by ImportVideo.
- Enforced exact accepted Asset ID, SHA-256, byte size and canonical relative
  original; different bytes fail before filesystem staging.
- Kept Relink semantically revision-free: no Clip, MediaSource, stream or time
  state changes.
- Rechecked accepted truth after copy and rolled back when a concurrent
  Revision changed the Asset contract.

# Verification

- Full macOS Visual: 61/61 passed.
- Unit receipts cover exact restore, identity mismatch, stale intent and
  concurrent-Revision rollback.
- Physical receipt deletes and restores the real imported MP4 while preserving
  the same accepted Revision and canonical project.
- Architecture: 132 source files, zero problems and zero visual-boundary debt.

# Claim boundary

This checkpoint does not provide a CLI import/relink client, MSVC receipt,
Timeline Video/Audio rows, Canvas decoded frame or Audio playback.

# Exact resume point

Add CLI import/relink clients over the shared services and one fail-closed
project-file persistence adapter. Then reproduce VI-WP01–03 under MSVC before
VI-WP04 exact scheduling and `DrawVideoFrame`.
