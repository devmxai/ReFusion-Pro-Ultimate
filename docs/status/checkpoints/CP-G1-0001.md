---
id: CP-G1-0001
kind: checkpoint
gate: G0-with-bounded-G1-macos-runtime
work_package: G1-WP01
commit: 4c91df7f6abf0734a47cfb549a726d15ef35281e
date: 2026-08-07
state: first-native-visual-slice-passed-macos-lifecycle-active-windows-not-run
---

# Completed

- Added a portable Viewport Presenter/Render Session contract with engine-owned
  frame index and presentation time.
- Added a Metal CAMetalLayer presenter isolated in the Apple platform adapter.
- Added same-device Skia drawable rendering with explicit HarfBuzz/ICU shaping.
- Embedded the engine viewport into the Qt shell through a QWindow host that
  owns no GPU, renderer, presenter, project state or render clock.
- Displayed and retained the first real macOS visual application artifact.
- Passed 10,000 real CAMetalLayer presentations with all forbidden CPU pixel and
  unattributed-copy counters at zero.

# Verification

See `docs/evidence/G1/G1-WP01.md` and
`docs/evidence/G1/artifacts/G1-WP01-verification.json`.

# Remaining G1-WP01 work

Capture physical occlusion, sleep/wake and device-loss behavior on MAC-LAB-001.
Do not close G1-WP01 or begin broad renderer/media scope from this visual slice.

# Exact resume point

Add lifecycle/device-generation diagnostics to the presenter session, execute
the physical macOS scenarios, and retain their counters. Continue keeping the
matching Windows contract and fixture unchanged; Windows remains `not-run` until
a physical runner/device is available.
