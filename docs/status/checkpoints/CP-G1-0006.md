---
id: CP-G1-0006
kind: checkpoint
gate: G0-with-bounded-G1-macos-runtime
work_package: G1-WP03
commit: 1433a576e75f47fa4377259da9c095eab20291a9
date: 2026-08-07
state: bounded-hardware-h264-decode-and-skia-gpu-composite-passed-macos
---

# Completed

- Added portable C++20 hardware-decode request/result, exact decoded-surface
  metadata and opaque native-surface lease contracts. Common code receives no
  CoreVideo, VideoToolbox, Metal, D3D or raw native surface handle.
- Added a 42,734-byte repository-owned synthetic H.264 High/Rec.709 corpus
  fixture with an exact manifest and SHA-256 integrity test. FFmpeg is neither a
  configure/build/runtime dependency nor a product decoder.
- Added an Apple decoder that parses only compressed Annex-B bytes on CPU,
  creates a VideoToolbox decompression session requiring hardware acceleration,
  and confirms the active session is hardware accelerated.
- Decoded source frame 3 with exact PTS `3/30` and duration `1/30` into a native
  NV12 video-range Rec.709 pixel buffer, without locking or mapping its pixels.
- Retained the output through an engine-owned opaque lease, bound both NV12
  planes through a CoreVideo texture cache created on the engine Metal device,
  verified adapter/generation identity, and proved deterministic lease release.
- Wrapped source frame 5 as Skia Ganesh YUVA textures and composited it into a
  private GPU render target without a CPU upload or GPU readback.
- Kept the matching Windows decoder factory and failure semantics portable; its
  implementation remains deliberately `not-qualified` until physical G1-WP04.

# Verification

- `macos-core`: 6/6 passed.
- `macos-core-sanitized`: 6/6 passed under ASan+UBSan.
- `macos-media`: 10/10 passed.
- `macos-visual`: 16/16 passed.
- Architecture Check: 41 owned files, zero problems.
- Docs Doctor: zero problems before this checkpoint update.
- Hardware receipt: one compressed sample, one hardware-decoded frame, two
  same-device plane bindings, one issued/released surface lease.
- Software decoder selections, CPU pixel maps/conversions/uploads, GPU
  readbacks, cross-adapter events and unattributed GPU copies: all exactly zero.

# Claim boundary

This qualifies a bounded all-IDR H.264 elementary-stream frame from compressed
bytes through VideoToolbox, CoreVideo/Metal and Skia GPU composition on the
physical macOS device. It is not MP4 demux, a decoder queue, decoded Timeline
playback, seek/B-frame/VFR qualification, Video Layer import, audio, export, or
Windows/iOS/Android runtime qualification.

# Exact resume point

Continue `G1-WP03` by replacing the one-request/one-IDR-session proof with a
bounded multi-frame decoder session and immutable PTS-indexed surface queue.
The existing engine transport must select that queue by its exact
`FixtureFrame.presentation_time_ns`; no UI timer or second media clock may be
introduced. Then add B-frame, long-GOP and non-zero/VFR timestamp fixtures and
prove flush/seek/lifecycle behavior while all forbidden counters remain zero.
