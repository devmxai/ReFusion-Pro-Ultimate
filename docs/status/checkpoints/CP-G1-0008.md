---
id: CP-G1-0008
kind: checkpoint
gate: G0-with-bounded-G1-macos-runtime
work_package: G1-WP03
commit: 25fcb394ff2be6f59c5468b4b46cddacc769a9b5
date: 2026-08-08
state: apple-bframe-vfr-long-gop-dependency-seek-passed
---

# Completed

- Added a portable hardware decode scheduler that maps exact presentation time
  to a compressed sample, walks decode order to the nearest sync dependency and
  delegates pixels only to the admitted platform hardware decoder.
- Added a portable Core-epoch/GPU-generation publication stamp. Completed work
  is rejected and its bounded queue released if either authority changes before
  publication; the scheduler owns no clock.
- Added explicit queue residency/backpressure telemetry and bounded retention.
  The long-GOP proof decoded 11 dependency samples, observed peak residency 11,
  retained two presentation surfaces and released nine before publication.
- Added a repository-owned 16-frame H.264 High/Rec.709 corpus row with real
  B-frames, two sync roots, VFR durations and a non-zero three-second source
  origin. Its Annex-B payload is locked by SHA-256.
- Proved three physical VideoToolbox seek/flush windows, stale Core epoch and
  device-generation invalidation, complete native lease release and Skia YUVA
  GPU composition of PTS-selected source frames `[9, 10, 9]`.

# Verification

- `macos-core`: 18/18 passed.
- `macos-core-sanitized`: 18/18 passed under ASan/UBSan.
- `macos-media`: 24/24 passed.
- `macos-visual`: 38/38 passed on the physical Metal/Skia lane.
- VideoToolbox: three hardware-required sessions, 19 submitted/decoded samples,
  three flushes, 38 plane bindings and 19/19 released native leases.
- Stale publication: one Core epoch rejection and one device-generation
  rejection; neither stale queue reached Skia.
- Software decode, CPU maps/conversions/uploads, GPU readbacks, cross-adapter
  events and unattributed GPU copies: all exactly zero.
- Architecture Check: 73 owned source files, zero problems before checkpoint
  documentation.
- Fixture SHA-256:
  `d73c301a64661a181a8dbd852a342a1c4526f880882664b8b0171d13dbec0e28`.

# Claim boundary

This passes the bounded Apple G1-WP03 hardware decode and dependency-seek proof.
It is not MP4/MOV demux, import, an accepted production Video Layer, audio,
export, Windows/iOS/Android runtime qualification or G1 exit.

# Exact resume point

Keep G1-WP03 closed. The next G1 engineering package is G1-WP05 typed GPU
observability/leases/fences/traces; the missing physical Windows lane still
blocks G0/G1 exit. Product Video/Audio import remains ordered under G4 after the
intervening authoring and visual-engine gates.
