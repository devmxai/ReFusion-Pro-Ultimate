---
id: CP-G1-0010
kind: checkpoint
gate: G0-with-bounded-G1-desktop-runtime
work_package: G0-WP05,G1-WP02,PLAN-XPLAT-FIX-001
commit: 235d67f900ac63b319502f3c5b140c90871d6760
date: 2026-08-09
state: windows-base-reconciled-portrait-ui-macos-green
tag: checkpoint-xplat-desktop-ui-20260809
---

# Completed

- Reconciled the physical Windows source/fix descendant through report commit
  `c89c792`; the tested Windows commit passed Core, D3D12 Graphics, Qt Visual and
  manual hardware Studio playback without WARP or CPU project pixels.
- Added the portrait/Reels workspace projection, enlarged native Canvas,
  compact color-coded Timeline tracks, owner-only top-level rows, simplified
  ruler, Space transport control and paused first-frame presentation.
- Kept UI as commands/snapshots only; accepted project and transport authority
  remain in Application/Core/Runtime.
- Accepted one canonical two-host Git workflow. Windows and macOS develop on
  classified branches; only one reviewed integration owner promotes `main`.

# Verification

- Final macOS Core: 30/30 passed.
- Final macOS Visual: 51/51 passed.
- `docs-doctor`: 110 documents, zero problems.
- `architecture-check`: 116 owned source files, zero problems and zero visual
  boundary debt.
- Windows tested source `1e2dc89`: Core 30/30, Graphics 38/38, Visual 44/44,
  physical Intel D3D12 Studio playback passed.

# Claim boundary

This is a development checkpoint, not a release. The new UI is physically
verified on macOS and still requires a same-commit Windows rerun. The formal
Metal/D3D12 calibrated pixel comparison, performance qualification, Windows
Media Foundation Video path, Android compile receipt, packaging and production
Export remain open. No branch or tag may describe those items as passed.

# Exact resume point

On Windows, pull the exact tagged `main`, run a clean baseline, and use a new
`evidence/windows-*` branch for the UI/Studio receipt or a new
`fix/windows-*`/`fix/shared-*` branch for any code correction. macOS reviews
and promotes accepted fixes through the canonical workflow.
