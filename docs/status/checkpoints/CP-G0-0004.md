---
id: CP-G0-0004
kind: checkpoint
gate: G0-with-G1-preflight
work_package: G0-WP03
commit: 075be91
date: 2026-08-07
state: clean-skia-macos-passed-windows-pending
---

# Completed

- Replaced placeholder-only GPU/Platform structure with Runtime GPU contracts,
  native Metal and D3D12 services, a Skia adapter, build profiles and tests.
- Removed all dependency on the deleted external Skia checkout.
- Materialized Skia, depot_tools and their official pinned dependency graph
  inside ReFusion only.
- Built a deterministic macOS arm64 Skia archive and passed same-device Ganesh
  and Graphite Metal context tests.

# Evidence

See `docs/evidence/G1/G1-PREFLIGHT-001.md` and source commit `075be91`.

# Known failures and risks

- G0-WP03 still needs an actual Windows portable-Core CI result.
- The D3D12 service and Windows Skia/Dawn profile are source-complete but have
  not compiled or run on Windows.
- G1 presentation, video-surface interop, timing, device loss and performance
  bake-offs remain mandatory.

# Exact resume point

Attach a Windows x64 runner. First execute `portable-core.yml`; then materialize
the same locked sources inside that checkout, build `windows-x64-d3d12`, run
`windows-graphics`, and record compiler, adapter, device and test evidence.

# Work that must not be repeated

- Never search for or reuse another Skia checkout.
- Never copy `out/deps-src` or `out/deps-build` from another machine/project.
- Never replace official GN/Ninja with generated Skia CMake source lists.
- Do not interpret macOS context creation as renderer or media qualification.
