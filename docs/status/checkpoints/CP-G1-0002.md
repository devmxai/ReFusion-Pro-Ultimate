---
id: CP-G1-0002
kind: checkpoint
gate: G0-with-bounded-G1-macos-runtime
work_package: G1-WP01
commit: b850a84ce8aff860bf8aac786440b396eb77024e
date: 2026-08-07
state: file-backed-30-second-project-playing-macos-g2-seed-only
---

# Completed

- Replaced the hard-coded application fixture with a real JSON project file
  opened from disk and admitted as one validated immutable Revision.
- Added portable Project/Composition/Layer/Transform/Keyframe contracts with
  stable IDs, a 1080x1920 Canvas and exact 30-second integer duration.
- Made Skia draw the opened Composition snapshot rather than embedded fixture
  constants.
- Added a Runtime-owned 30 fps render loop; Qt/QML has no project animation or
  repaint timer and only receives coalesced telemetry notifications.
- Displayed the opened project path, seven actual Layer tracks, live exact time,
  GPU frame count and loop index in Studio.
- Observed the project cross the 30-second boundary into Loop 2 and retained
  zero CPU pixel-transfer counters through a fresh 10,000-frame soak.
- Added an experimental machine-readable schema and proposed ADR-0008 so the
  seed is explicit and cannot be mistaken for a stable shipping format.

# Verification

- `macos-visual`: 11/11 passed.
- `macos-core`: 5/5 passed.
- `macos-core-sanitized`: 5/5 passed under ASan+UBSan.
- Architecture Check: 30 owned files, zero problems.
- Invalid project path: structured `RFX-PROJECT-OPEN`, process exit code 2.
- Presenter soak: 10,000/10,000 presented; all CPU pixel map/upload, GPU
  readback and unattributed-copy counters zero.

See `docs/evidence/G1/G1-WP01.md` and
`docs/evidence/G1/artifacts/G1-WP01-project-open-verification.json`.

# Claim boundary

The opened file is a real project and its Shape/Text animation is real engine
evaluation. It is not a decoded Video Layer, audio, save/reopen, agent live-file
reconciliation, export, stable project-format, G2 completion or cross-platform
qualification. Windows/iOS/Android remain `not-run`.

# Exact resume point

Finish physical macOS occlusion, sleep/wake and device-loss receipts for
G1-WP01. Do not broaden the experimental document seed until the G2 plan and
ADR-0008 decision explicitly define persistence, migration and transaction
semantics.
