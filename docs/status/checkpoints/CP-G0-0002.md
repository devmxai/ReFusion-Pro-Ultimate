---
id: CP-G0-0002
kind: checkpoint
gate: G0
work_package: G0-WP02
commit: b6cf27c
date: 2026-08-07
state: blocked-owner-decision
---

# Completed

- Activated MP-001 and the Product Contract.
- Accepted C++20, Qt command-only boundary, Skia semantic role, and strict
  hardware/zero-CPU-video-pixel policy.
- Created Qt license gate and module allow/deny policy.
- Created initial OS/media matrix and codec/font/Skia/Qt intake records.
- Enforced linked Qt modules and forbidden Studio APIs in architecture checks.

# Verification

- JSON policies parsed successfully.
- Docs Doctor passed 23 normative documents with zero problems.
- Architecture check scanned six owned source files, found only the five allowed
  Qt runtime targets, and reported zero violations.
- macOS Core and Studio workflows built; 1/1 test passed in each configuration.

# Blocking decision

The product owner must select `commercial` or `lgplv3-compliance` in ADR-0005.
No redistributable Qt artifact is authorized before this choice.

# Exact resume point

Record the owner's Qt lane in ADR-0005 and
`deps/policies/qt-modules.json`, then review ADR-0006/ADR-0007 and close
G0-WP02. Activate G0-WP03 only after that checkpoint.

# Work that must not be repeated

- Do not re-evaluate whether Qt owns project/render/media authority; it does not.
- Do not expand the media matrix before the initial profile qualifies.
- Do not download font binaries without an official immutable release and hash.
- Do not select a Skia backend before G1 measurements.

