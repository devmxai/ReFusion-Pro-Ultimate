---
id: CP-G1-0003
kind: checkpoint
gate: G0-with-bounded-G1-macos-runtime
work_package: G1-WP01
commit: 73d59a7960bae38ba7e2e9c41ae2df4898c8f565
date: 2026-08-07
state: gpu-lifecycle-fail-closed-and-physical-occlusion-passed-sleep-wake-pending
---

# Completed

- Added portable typed GPU health (`ready`, `suspended`, `lost`), lifecycle
  events, event sequence and generation matching.
- Made both declared Desktop adapters implement the same lifecycle contract:
  Metal observes native NSWorkspace notifications; D3D12 checks device removal
  after wake. Windows remains compile/runtime `not-run` on this Mac.
- Made `borrow()` reject while suspended/lost and made device loss advance the
  generation exactly once, invalidating every stale lease.
- Made Metal presentation reject a lost/stale generation and Runtime stop
  playback at the first rejected frame with its structured diagnostic.
- Added native NSWindow occlusion observation, separate from Qt visibility.
- Exposed read-only GPU lifecycle, visibility, occlusion and rejection counters
  in Studio.
- Physically covered the entire ReFusion window and restored it: occlusion
  suspend/resume reached `1/1`, visibility remained `0/1`, animation resumed and
  GPU stayed `READY`.

# Verification

- `macos-visual`: 12/12 passed.
- `macos-core`: 5/5 passed.
- `macos-core-sanitized`: 5/5 passed under ASan+UBSan.
- Native NSWorkspace will-sleep/did-wake notification test passed with event
  sequence 2 and unchanged generation.
- Injected device loss advanced generation by one; stale resource acceptance
  remained zero and the presenter rejected exactly once.
- Final presenter soak: 10,000/10,000 presented; CPU pixel maps/uploads, GPU
  readbacks and unattributed copies all zero.
- Architecture Check and Docs Doctor: zero problems.

# Remaining G1-WP01 work

The actual Mac has not been put to sleep during this checkpoint. One physical
sleep/wake cycle must show event sequence 2, `GPU READY`, resumed frame count,
unchanged generation and zero loss rejection. Do not replace this receipt with
the passing notification simulation.

# Exact resume point

Launch the current macOS visual build, put `MAC-LAB-001` to sleep and wake it,
then retain the post-wake telemetry/artifact. If the generation changes or the
presenter rejects, retain the diagnostic and repair the platform recovery path
before closing G1-WP01.
