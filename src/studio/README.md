# Studio adapters

Qt view models and command adapters belong here when extracted from the
foundation app. Project Canvas presentation remains engine-owned.

Inspector/Timeline controls are projections of portable descriptors and one
accepted bundle. Studio submits generic typed commands; it owns no FX defaults,
semantic validation, renderer dispatch, candidate activation or platform
fallback.
